<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginStart="16dp"
    android:layout_marginEnd="16dp"
    android:layout_marginTop="8dp"
    android:layout_marginBottom="8dp"
    app:cardCornerRadius="12dp"
    app:cardElevation="2dp"
    android:clickable="true"
    android:focusable="true"
    android:foreground="?android:attr/selectableItemBackground">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp">

        <!-- Image de la voiture -->
        <ImageView
            android:id="@+id/imgVoiture"
            android:layout_width="80dp"
            android:layout_height="60dp"
            android:scaleType="centerCrop"
            android:src="@drawable/car_placeholder"
            android:contentDescription="Voiture"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"/>

        <!-- Nom de la voiture -->
        <TextView
            android:id="@+id/tvNomVoiture"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="12dp"
            android:text="BMW 3 Series"
            android:textSize="16sp"
            android:textStyle="bold"
            android:textColor="#000000"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toEndOf="@id/imgVoiture"
            app:layout_constraintTop_toTopOf="@id/imgVoiture"/>

        <!-- Dates -->
        <TextView
            android:id="@+id/tvDates"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:text="15 Fév 2024 - 20 Fév 2024"
            android:textSize="13sp"
            android:textColor="#666666"
            app:layout_constraintEnd_toEndOf="@id/tvNomVoiture"
            app:layout_constraintStart_toStartOf="@id/tvNomVoiture"
            app:layout_constraintTop_toBottomOf="@id/tvNomVoiture"/>

        <!-- Prix -->
        <TextView
            android:id="@+id/tvPrix"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:text="350 €"
            android:textSize="16sp"
            android:textStyle="bold"
            android:textColor="#2962FF"
            app:layout_constraintStart_toStartOf="@id/tvNomVoiture"
            app:layout_constraintTop_toBottomOf="@id/tvDates"/>

    </androidx.constraintlayout.widget.ConstraintLayout>

</com.google.android.material.card.MaterialCardView>